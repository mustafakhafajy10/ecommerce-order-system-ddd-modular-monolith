Import this file into Postman:

E-Commerce-Order-System-Phase-2.postman_collection.json

Then use the collection runner on the imported collection.
It is already configured with:

- real seeded IDs
- request bodies
- test scripts for 200/201/400/409
- dynamic orderId capture from the create-order response

Requirements before running:

1. Docker containers must be running
2. API must be reachable at http://localhost:8080

